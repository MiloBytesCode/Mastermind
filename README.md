# Mastermind
Mastermind game repo for ZOGWIDROUHUA-11 group

# BUILDING
Use the command **>Cmake: build**
- this builds the projects and populates the `out` folder

# RUNNING
Currently, the build system is configured for debug compilation. 
After building, use the command **>Cmake: Debug** and the project
should run, producing errors if they exist